import React from 'react'
import { Collapse, List } from '@material-ui/core'

const ItemSubMenu = ({ icon, text }) => {

  return (
    <Collapse in={} timeout="auto" unmountOnExit>
      <List component="div" disablePadding>

      </List>
    </Collapse>
  )
}

export default ItemSubMenu
